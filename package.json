{
  "name": "zhibook-app",
  "version": "0.1.0",
  "description": "轻账 · Windows 桌面 + 移动 H5 双端个人记账工具",
  "private": true,
  "workspaces": [
    "packages/desktop",
    "packages/shared",
    "packages/server",
    "packages/mobile"
  ],
  "scripts": {
    "dev:server": "npm --workspace packages/server run dev",
    "dev:desktop": "npm --workspace packages/desktop run dev",
    "dev:mobile": "npm --workspace packages/mobile run dev:h5",
    "dev:shared": "npm --workspace packages/shared run build",
    "typecheck": "npm --workspace packages/desktop run typecheck && npm --workspace packages/shared run typecheck",
    "build": "npm --workspace packages/shared run build && npm --workspace packages/desktop run build",
    "dist": "npm run build && npm --workspace packages/desktop run dist"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "concurrently": "^9.1.0"
  }
}